// Jednobojni inline SVG ikoni (lucide). Boja preko `currentColor` — kontejneru daj
// `text-navy`/`text-orange`. Za inline u tekstu/gumbu: `inline-flex items-center gap-1.5`.
// Zamjena za emojije (konvencija loze; vidi BACKPORT-PLAN.md, izvor template 90f45da).
import {
  FileText,
  Handshake,
  MessageCircle,
  Search,
  Vote,
  type LucideIcon,
} from 'lucide-react';

export { FileText, Handshake, MessageCircle, Search, Vote };
export type { LucideIcon };
